# 流式 SSE、状态展示与角色确认门任务总结

## 已完成

- 已实现统一的流式事件契约：阶段归一化、时间戳、运行事件、角色图产物事件，以及稳定的 `run_id`/`turn_id` 关联。
- Node 服务端已支持 SSE 事件信封、单调事件 ID、进程存活期重放、断线不可重放提示、活动运行快照和本地安全 trace。
- Agent 已支持接收 Node 下发的运行 ID，使用模型真流式输出；不支持流式或流中异常时会按片段降级输出完整回答。
- 三条渲染流水线的角色肖像进度会实时转换为对话中的角色图产物。
- 前端已实现运行状态条、token 去重与排序、事件重放去重、角色图预览、角色版本分组展示和本地断线恢复。
- 已实现角色确认门：首次渲染仅生成基础肖像，原子写入会话级 `render_checkpoint.json`，并暂停后续分镜、关键帧和视频生成。
- 已实现角色编辑、重生成、确认与恢复：角色 ID 和既有镜头引用保持不变；新版本保存至 `character_portrait_versions/<role>/vN/`；所有角色确认后只恢复一次渲染。
- Node、stdin REPL、Agent 及前端确认卡已贯通；确认命令不会再交给 LLM 解释。
- 已完成 3 个主提交：`17e24cb`、`93a9216`、`7af4c1c`（此前 MVP 提交也保留在 `main` 历史中）。

## 未完成部分

- 未使用真实模型供应商、真实 API Key 或实际图像/视频生成服务进行端到端人工验收；本次仅使用测试桩、临时目录与伪造事件验证。
- Python 全量测试仍有 10 个既有失败，均已由任务方确认与本次改动无关：Windows 环境下的编码断言、GBK 读取 YAML、以及会话索引并发文件替换。
- 角色确认门的恢复依赖 Agent 进程仍存活；进程重启后 checkpoint 和肖像文件会保留，但不会自动继续渲染。

## 期间遇到的问题

- `uv run pytest` 在当前 Windows 环境无法规范化脚本路径，改用等价的 `uv run -- python -m pytest` 运行测试。
- 全量 Python 回归最初多出 4 个旧测试失败，原因是旧测试仍假设渲染会直接完成；已将本地忽略测试调整为新确认门语义，最终未引入额外失败。
- 默认系统编码为 GBK，导致部分历史测试和 YAML 读取失败；未为本次需求扩大修改范围处理这些既有问题。
- 确认门首轮结束后需要保留 SSE 活动快照，否则刷新页面会丢失“等待确认”状态；已让审批快照在首轮 `done` 后继续存在，并在真正恢复完成时清除。

## 未来优化方向

- 在隔离的测试环境配置专用假 Key 和 mock provider，补充三种工作流的浏览器级端到端验收。
- 为 checkpoint 增加显式的失败/可重试状态，并提供 Agent 重启后的安全恢复入口。
- 为会话状态文件引入跨平台文件锁与唯一临时文件名，解决 Windows 并发写入问题。
- 统一项目文件读取编码为 UTF-8，并修复历史英文断言与中文错误消息不一致的问题。
- 为角色图编辑增加更细粒度的版本历史、回滚和确认审计展示；必要时将运行与审批状态持久化到独立 API，而不只依赖存活进程的内存事件日志。
